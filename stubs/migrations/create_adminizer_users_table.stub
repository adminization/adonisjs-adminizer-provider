{{{
  exports({
    to: app.makePath('database/migrations', `${new Date().getTime()}_create_adminizer_users_table.ts`)
  })
}}}

import { BaseSchema } from '@adonisjs/lucid/schema'

export default class extends BaseSchema {
  protected tableName = 'adminizer_users'

  async up() {
    this.schema.createTable(this.tableName, (table) => {
      table.increments('id').notNullable()

      table.string('login', 254).notNullable().unique()
      table.string('full_name', 255).notNullable()
      table.string('email', 254).nullable().unique()
      table.string('password_hashed', 255).notNullable()
      table.string('api_key', 255).nullable().unique()
      table.string('avatar', 255).nullable()

      table.jsonb('widgets').nullable()

      table.string('locale', 10).nullable()
      table.string('timezone', 50).nullable()

      table.datetime('expires').nullable()

      table.boolean('is_deleted').notNullable().defaultTo(false)
      table.boolean('is_active').notNullable().defaultTo(true)
      table.boolean('is_administrator').notNullable().defaultTo(false)
      table.boolean('is_confirmed').notNullable().defaultTo(false)

      table.timestamp('created_at').notNullable()
      table.timestamp('updated_at').notNullable()
    })
  }

  async down() {
    this.schema.dropTable(this.tableName)
  }
}
